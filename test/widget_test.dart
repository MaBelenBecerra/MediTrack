import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Prueba inicial de configuración', () {
    // Esta es una prueba base para mantener el linter y el entorno limpios.
    // Como MediTrack usa Hive y Provider, las pruebas de widgets reales 
    // se implementarán en la fase de testing de la arquitectura.
    expect(true, true);
  });
}